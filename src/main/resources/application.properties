spring.mvc.hiddenmethod.filter.enabled=true
server.port=8081

# Finish in-flight requests before exiting on SIGTERM (container stop / rollout).
server.shutdown=graceful

# Exposes /actuator/health plus the liveness+readiness groups for orchestrators.
management.endpoint.health.probes.enabled=true
